# Copy this file to .env and fill in your values.
# Most configuration is done in config.yaml — this file is for secrets only.

# Optional: override the Ollama endpoint if not using localhost
# OLLAMA_ENDPOINT=http://localhost:11434
